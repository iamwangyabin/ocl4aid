# NoRGa prefix model and classifier-alignment settings.
prefix_len: 5
num_prefix_layers: 5
lam_orth: 1
ca_num_per_class: 200
ca_steps: 200
use_rp_gate: true
use_ema_head: false
rp_dim: 10000
rp_ridge: 10000
ema_ratio: [0.9, 0.99]
ensemble_method: softmax_max_prob
